import { defaultAnimationDelayLength } from './default';

const animationFrames = [{
  animationDelay: defaultAnimationDelayLength,
  canvasDepth: 1,
  text: [
    {
    	text: 'Welcome To',
    	fontSize: 25,
    	position: { y: -50 }
    },
    {
      text: 'Tuqire.com',
      fontSize: 83,
      position: { y: 22 }
    }
  ]
}];

export default animationFrames;
